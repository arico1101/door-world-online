# オンライン版のセットアップと当日の使い方

## 公開URL
https://tobira-online.tobira-online.workers.dev

Cloudflare Workers + Durable Objects で動いています(無料枠)。

## 当日の遊びかた
1. 進行役が公開URLを開き、なまえを入れて「ルームをつくる」→ 6文字のあいことばが出る
2. 参加者は各自のスマホで同じURLを開き、なまえとあいことばを入れて「ルームに参加」
3. 全員そろったら進行役が「ゲーム開始」
4. **家庭カードは、その人の端末にしか表示されません**(サーバーが本人にしか送らない)
5. 手番の人だけサイコロとトビラの選択ができ、ほかの人の画面には同じ内容が読み取り専用で映る
6. 全員ゴールで結果発表 → ネタバラシ → トビラ一覧 → ふりかえり

- 右上の🏠で、自分の家庭カードをいつでも見返せます
- 右上の？で、あそびかたを確認できます
- 日本語/English はロビー画面で切り替え

## 困ったとき
- **通信が切れた**: 自動で再接続します。画面を閉じてしまっても、同じ端末で同じURLを開けば元のプレイヤーとして戻れます
- **同じ端末で2人遊びたい**: URLの後ろに `?pid=なにか` を付けると別プレイヤーになれます(例: `...workers.dev/?pid=a`)
- **やり直したい**: 結果画面で進行役が「もういちど遊ぶ」を押すと、同じメンバーでロビーに戻ります
- **人数**: 2〜6人。開始後は新しい人は入れません(入り直しは可能)

## 開発メモ
```bash
npm install          # 初回のみ
npx wrangler dev     # ローカルで起動 (http://localhost:8787)
npx wrangler deploy  # 本番へ反映
node test/play.mjs   # 通しプレイの自動テスト(N=人数, HOST=接続先)
```

- `src/index.js` … Worker + Durable Object(ゲームの判定はすべてここ)
- `public/rules.js` … サーバーと画面で共有するルール・文言(1画面版と同一に保つ)
- `public/app.js` … 画面と入力だけを担当するクライアント
- `public/index.html` … 画面の骨格とCSS

### 設計の要点
- **家庭カードの秘密**: サーバーは各接続に、その人のカードだけを送る。公開されるのは名前・色・位置・おかね・まなび・♥のみ。結果発表で全公開に切り替わる
- **判定はすべてサーバー**: クライアントを改造しても、他人のカードは見えず、手番も奪えない
- ゲームバランス・文言は1画面版(door-world-game)と同一。ルールを変えるときは両方に反映する
